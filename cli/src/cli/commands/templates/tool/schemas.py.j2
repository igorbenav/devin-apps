"""Pydantic schemas for the << label >> tool."""

from datetime import datetime

from pydantic import BaseModel, ConfigDict, Field

from .models import STATUS_OPEN


class << class_prefix >>ItemBase(BaseModel):
    title: str = Field(min_length=1, max_length=200)
    notes: str | None = None


class << class_prefix >>ItemCreate(<< class_prefix >>ItemBase):
    model_config = ConfigDict(extra="forbid")

    status: str = STATUS_OPEN


class << class_prefix >>ItemUpdate(BaseModel):
    model_config = ConfigDict(extra="forbid")

    status: str


class << class_prefix >>ItemRead(<< class_prefix >>ItemBase):
    id: int
    status: str
    created_at: datetime
